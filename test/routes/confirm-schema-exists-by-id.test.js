describe('routes/confirm-schema-exists-by-id', () => {
    it('Should continue to next step if schema is found.');
    it('Should fail with "404 Not found" if schema is NOT found.');
    it('Should fail with "500 Internal error" if an exception occurs.');
});
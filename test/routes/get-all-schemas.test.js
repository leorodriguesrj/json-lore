describe('routes/get-all-schemas', () => {
    it('Should respond with "200 ok" and body if collection contains itens.');
    it('Should respond with "200 ok" and empty list if database is empty.');
    it('Should respond with "500 Internal error" in case of exception.');
});
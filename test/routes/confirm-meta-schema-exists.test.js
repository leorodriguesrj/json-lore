describe('routes/confirm-meta-schema-exists', () => {
    it('Should continue if meta-schema is found');
    it('Should fail with HTTP 400 if meta-schema is not found');
    it('Should fail with HTTP 500 if an exception is thrown');
});
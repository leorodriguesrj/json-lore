describe('routes/confirm-payload-integrity', () => {
    it('Should continue to next step if validation succeeds');
    it('Should fail with "400 Bad request" if validation fails');
    it('Should fail with "500 Internal erro" if exception occurs');
});